import 'package:flutter/material.dart';

class Palette {
  // region Common
  static const white = Colors.white;
  static const black = Colors.black;
  static const transparent = Colors.transparent;

  // TODO: Change value to proper value
  static const profileNameBackgroundColor = Color(0xff000000);

  // endregion

  // region Cases
  static const danger = Color(0xFFFF5252);
  static const ok = Color(0xFF4CAF50);
  static const neutral = Color(0xFF607D8B);

  // endregion

  // region App (Light Theme)
  static const primaryColor = black;
  static const secondaryColor = Color(0Xff1A1A1A);
  static const scaffoldBackground = white;
  static const gradientColor = Color(0xffC4C4C4);
  static const appBarBackground = transparent;
// endregion
}
